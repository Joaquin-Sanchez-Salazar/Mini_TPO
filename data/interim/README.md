# Datos intermedios

Contenido generado por `run_preparation()` y los notebooks 01/02. No se versiona porque es reproducible y puede contener información derivada fila a fila.

Salida principal esperada:

- `base_mini_tpo_clean_full.parquet`.

Requiere previamente los archivos documentados en `data/raw/README.md`.
